# 微光同行 - UI更新日志
> 日期：2026-07-21
> 版本：v1.3 UI优化版
> 编译状态：BUILD SUCCESSFUL

---

## 一、家属端/详情页添加返回按钮

### 修改文件
- `app/src/main/java/com/weiguangchangxing/tonghang/sos/SosAlertActivity.kt`
- `app/src/main/java/com/weiguangchangxing/tonghang/FamilyModeActivity.kt`

### 修改内容
1. **SosAlertActivity.kt**：SOS告警详情页顶部添加了返回箭头按钮（ArrowBack图标），点击后调用 `activity.finish()` 返回上一页
2. **FamilyModeActivity.kt**：家属端联动查看页面顶部添加了返回箭头按钮，点击后调用 `finish()` 返回主界面

### 技术细节
- 新增导入：`androidx.compose.material.icons.Icons`、`androidx.compose.material.icons.filled.ArrowBack`、`Icon`、`IconButton`
- FamilyModeScreen 新增 `onBack: () -> Unit` 回调参数

---

## 二、首次使用引导弹窗

### 修改文件
- `app/src/main/java/com/weiguangchangxing/tonghang/core/AppPreferences.kt`
- `app/src/main/java/com/weiguangchangxing/tonghang/MainActivity.kt`

### 修改内容
1. **AppPreferences.kt**：新增 `hasSeenFirstUseGuide()` 和 `markFirstUseGuideSeen()` 方法，基于 SharedPreferences 记录首次引导状态
2. **MainActivity.kt**：
   - 入口选择页面 `showEntryScreen` 改为仅首次启动时显示（`!preferences.hasSeenFirstUseGuide()`）
   - 新增 `showFirstUseGuideDialog` 状态变量
   - 在 `LaunchedEffect` 中添加首次引导弹窗检查
   - 添加首次引导 AlertDialog，介绍微光同行6大核心功能
   - "选择模式"按钮：关闭弹窗 → 进入用户端/家属端选择页
   - "直接进入"按钮：关闭弹窗 → 直接进入主界面
   - 入口选择页面的两个按钮（用户端/家属端）均会标记首次引导已看

### 技术细节
- 使用 SharedPreferences key `first_use_guide_seen` 持久化存储
- 引导弹窗内容包括：OCR识别、语音识别、SOS求助、手语交互、震动闹钟、商品识别

---

## 三、震动闹钟快速触发按钮

### 修改文件
- `app/src/main/java/com/weiguangchangxing/tonghang/MainActivity.kt`

### 修改内容
- 在震动闹钟卡片（双向手语交互旁边）中新增"短震动 (5秒)"快捷按钮
- 点击后直接调用 `vibrationController.vibratePreset(VibrationController.PRESET_SHORT)`
- 按钮使用 `tertiaryContainer` 配色，与开关区分

### 技术细节
- Button 位于 Switch 开关上方，便于快速触发
- 无需开启震动闹钟开关即可使用短震动按钮

---

## 四、删除朗读引擎状态卡片（TtsStatusCard）

### 修改文件
- `app/src/main/java/com/weiguangchangxing/tonghang/MainActivity.kt`

### 修改内容
1. 从菜单栏 `AnimatedVisibility` 中删除了 `TtsStatusCard(...)` 调用（4行）
2. 删除了 `TtsStatusCard` 函数定义（约43行，含朗读引擎状态、切换男声/女声、下载语言包等UI）

---

## 五、删除无障碍权限向导

### 修改文件
- `app/src/main/java/com/weiguangchangxing/tonghang/MainActivity.kt`
- `app/src/main/java/com/weiguangchangxing/tonghang/ui/settings/SettingsScreen.kt`

### 删除内容
1. **状态变量**：删除 `showAccessibilityGuideDialog` 和 `showAccessibilityRequiredDialog`
2. **LaunchedEffect 检查**：删除无障碍权限向导弹窗触发逻辑
3. **AlertDialog**：删除"无障碍权限向导"和"需要先开启无障碍"两个弹窗（约55行）
4. **UI 组件**：删除 `AccessibilityGuideCard` 组件调用（约7行）
5. **FAB 按钮**：删除右下角"无障碍"悬浮快捷按钮（约15行）
6. **函数定义**：删除 `AccessibilityGuideCard` 函数定义（约40行）
7. **回调参数**：删除 `onOpenAccessibilitySettings`、`onReadAccessibilityState`、`onAccessibilityQuickToggle` 三个回调参数及其实现
8. **SettingsScreen.kt**：删除 `onOpenAccessibilitySettings` 参数和"无障碍服务设置"按钮

### 影响范围
- 总共删除约 160+ 行代码
- 5个文件被修改（MainActivity.kt 为主要修改对象）

---

## 编译结果

```
BUILD SUCCESSFUL in 2m 25s
44 actionable tasks: 14 executed, 30 up-to-date
```

无编译错误，仅存在少量预先存在的警告（未使用参数、已弃用API等）。

---

## APK 输出路径

`f:\java\weiguangplus\2\app\build\outputs\apk\debug\app-debug.apk`